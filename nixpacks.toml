[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["cd FieldServiceCRM && dotnet restore && dotnet publish -c Release -o out"]

[start]
cmd = "cd FieldServiceCRM/out && dotnet FieldServiceCRM.dll --urls http://0.0.0.0:8080"