[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["cd FieldServiceCRM && dotnet restore"]

[phases.build]
cmds = ["cd FieldServiceCRM && dotnet publish -c Release -o out"]

[start]
cmd = "cd FieldServiceCRM/out && dotnet FieldServiceCRM.dll --urls http://0.0.0.0:$PORT"